# 开发笔记：Economist PDF 网页阅读器

这份文档记录这个项目的设计思路、遇到过的 bug 和排查过程、用户反馈，以及后续所有改动的记录。**以后任何修复/改动都追加写在本文档末尾的"变更记录"里**，不要另开新文档。

## 项目目标

把《经济学人》PDF 杂志（`PDF/` 目录下）变成一个可以"一篇文章一篇文章"阅读的网页：先看目录（按栏目分组），点进某篇文章才展开正文，不是把整本杂志堆在一屏。目的是配合 Chrome 浏览器插件（如 Kimi）查词、理解语义，所以正文必须是可选中的真实文本，不能是 PDF 截图/canvas。

用户要求：
- 生成物和 `PDF/` 原始文件分开存放；
- 方案要可复用：以后丢新一期 PDF 进 `PDF/`，重新跑一下提取脚本就能生成新一期的阅读页；
- 省 token、性价比高：不用 Claude 逐页"看图读PDF"，改用本地 Python 脚本一次性把文字提取好。

## 整体架构

```
PDF/<issue>.pdf
      │  python3 scripts/extract_articles.py "PDF/<issue>.pdf"
      ▼
reader/data/<slug>.js      每期一份，window.ISSUES['<slug>'] = {title, articles:[...]}
reader/data/manifest.js    已处理过的期刊列表，window.ISSUE_MANIFEST = [...]
      │
      ▼
reader/index.html + app.js + style.css   纯静态 SPA，hash 路由：
   #/                        目录首页（列出已处理的期刊）
   #/issue/<slug>            该期目录页，按栏目分组列出文章标题+副标题
   #/issue/<slug>/article/<id>  单篇文章阅读页，带上一篇/下一篇/返回目录
      │  git push
      ▼
.github/workflows/pages.yml   push 到这个分支时自动把 reader/ 发布到 GitHub Pages
      ▼
https://awayicheng-tammy.github.io/English/   实际打开阅读的网址
```

关键设计决定：
- **文章数据用 `<script src="data/xxx.js">` 注入为 JS 全局变量**，不用 `fetch()` 读 JSON——本来是为了绕开 `file://` 协议下 fetch 的 CORS 限制（后来发现 file:// 还有更大的问题，见下方"决定 2：改用 GitHub Pages"）。
- **用 GitHub Actions 把 `reader/` 部署到 GitHub Pages**，而不是本地双击打开：因为 Chrome 默认禁止所有浏览器插件在 `file://` 本地文件上运行（用户需要在 chrome://extensions 手动给每个插件开"允许访问文件网址"，而且有些插件开了也不行），这样 Kimi 插件才能正常查词。工作流直接部署这个开发分支（`claude/economist-pdf-web-reader-zn8j39`），不需要先合并到 `main`——GitHub Pages 的 Actions 部署方式本来就和"默认分支"无关，只要仓库 Settings → Pages 里把 Source 设成 "GitHub Actions" 即可（这一步需要仓库所有者手动点一次，工具权限做不到）。
- `open-reader.html`（仓库根目录）：双击可直接跳转到上面那个 GitHub Pages 网址，省得每次都要记网址。

## 提取脚本 `scripts/extract_articles.py` 的设计思路

PDF 没有内嵌书签/大纲（`doc.get_toc()` 是空的），所以文章切分完全靠版式规律（字号、字重、位置）来猜，而不是靠明确的结构化数据。核心逻辑：

1. 用 PyMuPDF 的 `get_text("dict")` 拿到每页的文字块，含每个文字段落（span）的字号、字体、坐标。
2. **判定"标题块"（banner）**：一个文字块里如果有字号 ≥ 14pt 的内容、且这部分大字总字数 ≥ 4 个字符（排除首字下沉的单字符干扰），就判定为标题/副标题/栏目名，标志着一篇新文章的开始。
3. **栏目名识别**：维护一份《经济学人》固定栏目名单（Leaders、Britain、Business……），在页眉或独立的栏目标签块里匹配到就更新"当前栏目"。
4. **正文分栏排序**：杂志是多栏排版，正文块必须先按列（x 坐标聚类）分组，同一列内按 y 坐标从上到下排序，不能简单按 y 坐标整页排序（否则左右栏顺序会错乱）。
5. **文章结束信号**：《经济学人》在很多文章末尾印一个"■"实心方块作为结束标记（这是用户提醒我使用的信号，见下方变更记录）——见到就立即结束当前文章，不再把后面的内容黏到已经写完的文章上。
6. 把提取到的文章按 `{id, section, title, dek, pageStart, pageEnd, paragraphs}` 结构写进 `reader/data/<slug>.js`。

这套方法本质上是启发式的，不追求第一次就 100% 准确——原则是"先做出来，用户读的时候发现问题，再针对性修"。下面的变更记录就是这个"边读边修"过程的完整历史。

## 已知仍未解决的问题（截至最新一次改动）

- **"Eating bitterness"、"Yielding" 两篇文章开头是从句子中间开始的**：真正的开头被错误地并入了前一篇文章的结尾。初步怀疑还是分栏/分块边界问题，但具体成因还没抓到，需要单独用类似前面几次的调试方法（打印该页所有文字块的 bbox/字号）去定位。
- **少数"一页塞好几篇短文"的版式会把 2-3 篇短文章合并到一个标题下**：具体是 Britain 版"Back to the shires"(板球) 被合并进"Two toots for the planning overhaul"、Science & technology 版"Make it stick"被合并进"Should you stand at your desk?"、Culture 版"Vin-dinavia"被合并进"Get the picture?"。这些页面的版式是标题和对应正文的位置关系不满足"标题必然在其正文前面"这个基本假设（一个"盒装"小文章的标题印在页面下方而正文在别处），需要更复杂的空间分区逻辑才能解决，目前没有修。
- **个别文章可能仍缺副标题**（如"Without a trace"）：说明 SemiBold 字重这个判定信号不是百分之百覆盖所有情况，具体原因还没细查。
- **极少数复合词连字符判断仍会出错**（约 6 处/全书）：`switch-gears`、`liberal-isers`、`glue-ball`、`point-defence`、`weapons-engagement`、`with-drawfrom`，详见"修复六"。其中 `with-drawfrom` 本质是下面这条"缺空格"问题的连带表现。
- **少数地方两个独立单词之间缺空格被粘在一起**（如 "parliamentary elections" 被提取成 "parliamentaryelections"）：这是 PDF 排版紧凑导致的老问题，`fix_glued_words()` 目前只覆盖了标点/引号/称谓后紧跟大写字母这几种可以安全判断的模式，普通单词间纯粹因为字距过紧缺空格、又没有标点做锚点的情况还没处理，也没有可靠信号能和"连字符复合词"问题区分开（修复六调试时发现的）。

## 变更记录

### 2026-09-08 初版：提取脚本 + 网页阅读器
- 新建 `scripts/extract_articles.py`、`reader/`（index.html/app.js/style.css/data/）、`requirements.txt`。
- 按字号启发式把 PDF 切成约 68 篇文章，按栏目分组展示，SPA 形式一篇篇打开。
- PR #1，用户已合并到 main。

### 2026-09-08 修复一：两栏阅读顺序错误
- **现象**：用户反馈《Could AIs become conscious?》第一段读到的不是开头，而是中间偏后的一句。
- **根因**：`column_sort()` 把**页面上所有文字块**（含标题、正文）都拿来做 x 坐标聚类分栏，再按 `(列号, y0)` 排序。标题块的 x 坐标经常和任何一栏正文都对不齐（标题经常居中或跨栏），于是被聚成自己独立的一"列"，排序时这个"列"被排到了整个左栏之后——导致标题触发"新文章"的时机被推迟到左栏正文读完之后，左栏正文被错误地挂到了上一篇文章尾部。
- **修复**：把"判断标题/栏目名"和"给正文分栏排序"分开——标题类的块（banner）保持原始从上到下的顺序，不参与分栏；只有正文块（body）才用 x 坐标聚类分栏。顺手把 `COLUMN_GAP` 从 30 调大到 90（同栏缩进块最大约 63.5pt 偏移，真实栏间距最小约 129pt，90 正好卡在中间）。
- 同时改成部署 GitHub Pages（见上方"决定 2"），因为发现 Kimi 插件在 `file://` 本地文件上完全读不到内容。
- PR #2，用户已合并。

### 2026-09-08 修复二：首字下沉字母丢失
- **现象**：用户反馈第一篇文章第一段应该是 "MOST PEOPLE believe..."，读到的却是 "OST PEOPLE believe..."，少了开头的大写字母。
- **根因**：首字下沉（drop cap）的那个大字符单独是一个文字块，PyMuPDF 报告的这个块的 bbox 顶部 y 坐标有时会比它自己所在段落的 bbox 顶部**低零点几个像素**（字体度量的怪癖），导致排序时段落排到了字母前面；而这个单字符块又因为"文字长度 ≤ 2"被当作噪声直接丢弃。
- **修复**：识别出"看起来像首字下沉"的块（字号 ≥ 14pt 且文字长度 ≤ 2）时，排序阶段把它的 y 坐标人为减小 5pt，确保排到自己段落前面；同时不再丢弃这个字符，而是缓存下来拼接到紧跟着的下一段正文开头。
- PR #3（结果被用户很快合并，导致后面又出现"我推送的最新一次修复不在 PR 里"的情况——这变成了一个模式，见下方"工作流约定"）。

### 2026-09-08 修复三：用文章结束符号"■"做硬边界
- **触发**：**用户主动提醒**——他发现文章结尾经常有一个实心黑方块"■"，提示我可以用这个来辅助判断文章分界。
- 这是《经济学人》自己的排版惯例："■"是印刷版本身就有的"文章结束"标记（不是提取错误）。全书 59 处这个符号，绝大多数文章末尾都有。
- **第一版实现踩了一个坑**：加了"见到■就把 current 设为 None"的逻辑，但忘了在关闭的同时把这篇文章真正 `finalize + append` 进结果列表——导致所有在文章中间被"■"提前关闭、且后面还有更多本该属于同一篇的内容的情况下，这篇文章因为从来没被 append 过而彻底消失。文章数从 65 骤降到 16。用调试脚本手动复现每一步（打印每个 block 处理时的 banner/body 判断结果）才定位到：`current["paragraphs"]` 累积没问题，唯独漏了 append。修好后（见到"■"就立刻 `finalize_article` + `append`，再把 `current` 置空）恢复到 65 篇。
- 同时把"■"本身从展示文本里去掉，阅读更干净。
- PR #3（这次真正生效的版本）。

### 2026-09-08 修复四：整页文字丢失（"Flirting with the next intifada"整篇消失）
- **触发**：**用户对照纸质/PDF原文发现少了一整篇《Flirting with the next intifada》**，问我是不是没提取到。
- **根因排查过程**：
  1. 先确认这篇文章标题、副标题、正文在 PDF 原文里确实存在（直接搜索"intifada"、"Flirting"定位到页面）。
  2. 发现用来做"标题/副标题/正文分类"的 `get_text("dict")`（拿字号信息）和用来做"干净文本"的 `get_text("blocks")`（simple 模式，拿正确的单词间距）这两套 PyMuPDF 提取结果，本来假设是一一对应、数量相同的（脚本里原来有一句防御性检查：`if len(dict_blocks) != len(simple_blocks): continue`，数量对不上就整页跳过）。
  3. 这一页恰好 dict 模式切出 25 个文字块，blocks 模式只切出 24 个——两种模式各自的分块算法不一致，数量对不上，于是这句防御检查把**整页内容**都跳过了，连带把这篇文章一起丢了。
  4. 进一步测了 `get_text("words")`（单词级别，间距最准）发现它的内部分块编号（block_no）又是第三套不同的编号方式，三种模式互相都对不上——说明"用某种模式的块编号去匹配另一种模式的块列表"这个思路本身就不可靠，不是这一处巧合。
- **修复**：不再依赖任何一种模式自己的块编号去互相匹配，改成**用坐标位置（bbox）做空间归属**——把整页的 `words()` 结果按照"中心点落在哪个 dict 块的框里"分配给对应的 dict 块（如果一个词的中心点同时落在多个块的框里，归给面积最小的那个，避免首字下沉的大字符框和它旁边段落的框有重叠时被两边重复计入）。这样彻底不需要"两种模式块数量对不上就跳过整页"这句防御代码了，直接删掉。
- **顺带修复的连带问题**：改成坐标归属后发现页眉文字的真实左右顺序在奇偶页是相反的——奇数页（右页）是"The Economist 日期 栏目名 页码"，偶数页（左页）是"页码 栏目名 The Economist 日期"（这是杂志排版"外侧页边留白放栏目名"的惯例）。原来的正则只认其中一种顺序，导致这次改动后所有栏目名一度被识别成上一个栏目（因为正则彻底匹配不上，`current_section` 从来没更新过）。改成先找到日期短语的位置，再独立在剩余文字里找"孤立的 1-3 位数字"当页码，剩下的部分才是栏目名——不再假设固定的左右顺序。
- PR #4。

### 2026-09-08 修复五：部分文章副标题缺失
- **触发**：用户发现目录页有些文章显示副标题，有些不显示，问是什么问题。
- **根因**：副标题（dek）和正文字号一样大（都是 9-9.5pt），没法靠字号区分。原来的逻辑只在"标题所在的同一个文字块"里找副标题（同一块里字号更小的那几行）——如果 PDF 把副标题单独切成了一个块（不和标题合并），这个副标题就会被当成正文的第一段处理掉，导致文章有真实副标题却没显示出来（副标题文字变成了正文第一段开头，比如"Flirting with the next intifada"这篇的正文第一段一度是"America must press..."这句本该是副标题的话）。
- **修复**：发现单独成块的副标题有一个可靠的区分特征——几乎全篇用 SemiBold（半粗体）字重，而正文是 Light 字重（除了偶尔开头几个字用大写强调）。加了一条规则：一篇文章标题后遇到的**第一个**正文级别大小的块，如果几乎全是 SemiBold，就当作副标题，不算进正文段落。
- 有副标题的文章从 47 篇增加到 51 篇。
- PR #5。

### 2026-09-08 修复六：行尾连字符误删（"year-olds" 被拼成 "yearolds"）
- **触发**：用户发现《Could AIs become conscious?》里 "18- to 29-year-olds" 被拼成了 "18- to 29-yearolds"。
- **根因**：原文换行是 "18- to 29-year-\nolds"——这次换行恰好落在真正的复合词连字符上。`dehyphenate_join()` 原来的逻辑是"只要行尾是字母+连字符、下一行开头是小写字母，就无条件去掉连字符拼接"，这个假设对绝大多数"排版换行硬插的连字符"（如 "de-\ncade" → "decade"）是对的，但对恰好在连字符处换行的真复合词就会拼错。
- **踩过的坑（原生词典频率法不够用）**：一开始尝试"把两段拼起来查词频，词频低就说明不是一个真词、应该保留连字符"这个最直接的思路。实测扫了全书约 1905 处行尾连字符，"拼起来查词频很低"的有 152 处——但逐条看下来发现里面绝大多数根本不是复合词判断问题：
  - **约 40 处是人名/专有名词被换行截断**（如 "Cof-nas"→应该是"Cofnas"、"Jen-rick"→"Jenrick"、"Zelen-sky"→"Zelensky"），人名本来就是低频词，会被误判成"保留连字符"，反而把原来正确的结果改错。
  - **约 15-20 处其实是另一个不相关的 bug**——两个独立单词因为 PDF 排版紧凑被粘在一起、中间缺空格（如 "parliamentary elections" 被提取成 "parliamentaryelections"），这跟连字符判断没关系，纯查词频只会得到误导性的低分。
  - **还有一批是词典本身覆盖不全的真实单词**（尤其是英式 -isation/-ised 拼法，如 "denuclearisation"、"professionalised"、"monopolised"），词频虽不算高但确实是合法单词，会被误判成复合词。
- **修复**：把判断逻辑收紧成三个信号同时满足才保留连字符：① 连字符前的那段文字本身**不能是大写开头**（排除人名）；② 连字符前的那段文字长度 ≥ 3 且**本身就是一个常见完整单词**（`zipf_frequency ≥ 3.5`，排除"de"、"con"这类音节碎片——这类碎片本身词频也不低，因为是常见的短字母组合，所以只在末尾单独判断这一步不够，见下）；③ 两段直接拼起来（不带连字符）后**几乎不构成一个真实单词**（`zipf_frequency < 0.5`）。三个条件同时满足才认为是真复合词、保留连字符，否则按老逻辑去掉连字符拼接。
- 用这套组合规则重新扫描后，"保留连字符"的候选从 152 处收窄到 49 处，人工抽查大部分（"year-olds"、"state-built"、"long-serving"、"third-best"、"purpose-built"、"knock-on"、"hard-left"、"far-right"、"one-off"、"toll-free"、"mass-market"、"state-owned"、"second-largest"、"two-thirds" 等）都是真实需要连字符的复合词。
- **仍然承认存在的残留误差**（抽查发现约 6 处，全书 320000+ 字符里占比很小）：`switch-gears`（应为一个词 switchgear）、`liberal-isers`（应为 liberalisers）、`glue-ball`（物理学术语应为 glueball）、`point-defence`、`weapons-engagement`（这两个作为名词短语通常不加连字符）、`with-drawfrom`（这条其实是前面提到的"缺空格"那个不相关 bug 导致的，本该是"withdraw from"两个词，无论加不加连字符都是错的）。这类误差本质上是"英文里到底该连字符还是该空格还是该合成一个词"这个问题在没有完整词典/NLP 工具的情况下很难 100% 判准，选择接受这个小比例的残留误差、换取"18- to 29-year-olds"这类高频真实场景被修好。
- `requirements.txt` 新增 `wordfreq` 依赖（纯 Python、自带词频数据、不用联网）。
- PR #7。

## 工作流约定

- 每次修复后 `git commit` + `git push` 到分支 `claude/economist-pdf-web-reader-zn8j39`。
- **用户会很快自己在 GitHub 上把 PR 合并掉**，所以推送新提交后如果发现上一个 PR 已经是 merged 状态，要**主动开一个新 PR**，不用等用户开口确认（这是用户明确要求的）。
- GitHub Pages 用 GitHub Actions 部署（`.github/workflows/pages.yml`），直接从这个开发分支部署，不需要先合并到 main；仓库 Settings → Pages 的 Source 需要设成"GitHub Actions"，以及 Settings → Environments → `github-pages` 的 "Deployment branches and tags" 需要设成"No restriction"（这两步是仓库所有者一次性手动设置的，工具权限做不到）。
- 每次改完 `scripts/extract_articles.py` 后：重新跑一遍提取脚本、用 Playwright 无头浏览器走一遍首页→选刊→点文章→翻页→返回目录确认没有报错、扫一遍所有文章开头有没有出现新的"小写字母开头"或"字母重复"之类的回归信号，再提交推送。
