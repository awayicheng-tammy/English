# 操作清单

对照着一步步做，做完一步勾一步。

几个固定信息，下面会反复用到：

| 项目 | 值 |
|---|---|
| GitHub 仓库 | https://github.com/awayicheng-tammy/English |
| 要上传到的分支 | `claude/economist-pdf-web-reader-zn8j39`（**不是 main**） |
| 部署状态页 | https://github.com/awayicheng-tammy/English/actions |
| 阅读网站 | https://awayicheng-tammy.github.io/English/ |

> **PDF 只留在自己电脑上，不要上传到 GitHub。** 网页上传单个文件最大 25MB，杂志 PDF 往往超过这个大小。网站只需要脚本生成的 `.js` 数据文件。

> 下面命令里的 `python3`：Mac 用 `python3`；Windows 一般用 `python`（或 `py`）。

---

## A. 新增一期杂志

### 1. 拿到最新版的脚本
每次处理新一期之前都做一遍。脚本会不断修 bug，旧脚本生成的文章会带着已经修好的问题。

- [ ] 打开 GitHub 仓库，左上角的分支下拉框切换到 `claude/economist-pdf-web-reader-zn8j39`
- [ ] 进入 `scripts/` → 点开 `extract_articles.py` → 点右上角的下载按钮（Download raw file）
- [ ] 用下载的文件替换本地文件夹里的 `scripts/extract_articles.py`
- [ ] 同样方法下载根目录的 `requirements.txt`，替换本地的同名文件

（如果本地文件夹是用 `git clone` 下载的，上面几步可以换成在文件夹里执行一条 `git pull origin claude/economist-pdf-web-reader-zn8j39`）

### 2. 安装/更新依赖
- [ ] 打开终端，进入本地仓库文件夹（`cd` 到 `English` 文件夹）
- [ ] 执行：
  ```
  pip install -r requirements.txt
  ```
  ✅ 最后没有红色 `ERROR` 就行（显示 "already satisfied" 也没问题）

### 3. 放入 PDF
- [ ] 把新一期的 PDF 放进本地的 `PDF/` 文件夹
- [ ] 文件名按这个格式命名：`The_Economist_UK_-_22_August_2026.pdf` 或 `The_Economist_Europe_-_29_August_2026.pdf`
  网站上显示的期刊名就是从文件名来的。

### 4. 运行提取脚本
- [ ] 执行（把文件名换成你的）：
  ```
  python3 scripts/extract_articles.py "PDF/The_Economist_UK_-_5_September_2026.pdf"
  ```
  ✅ 看到 `Wrote 6X articles to ...`，下面列出各栏目的文章数

### 5. 本地先看一眼
- [ ] 双击打开本地的 `reader/index.html`
  ✅ 首页列表里出现了新一期，点进去能看到目录和文章
  （这个本地页面里 Kimi 插件用不了，只是用来检查；正式阅读要用网站）

### 6. 上传生成的文件到 GitHub
只需要上传两个文件，都在本地的 `reader/data/` 里：
- `manifest.js`（期刊列表）
- 新生成的那一期 `.js`，比如 `the-economist-uk-5-september-2026.js`

- [ ] 打开 GitHub 仓库，**分支下拉框切换到 `claude/economist-pdf-web-reader-zn8j39`**
- [ ] 进入 `reader` → `data` 文件夹（**一定要先进到这个文件夹里再上传**，否则文件会传到错误的位置）
- [ ] 点 `Add file` → `Upload files`，把上面两个文件拖进去
- [ ] 页面下方选 **Commit directly to the `claude/economist-pdf-web-reader-zn8j39` branch**
- [ ] 点 `Commit changes`

### 7. 确认网站已更新
- [ ] 打开部署状态页（Actions），最上面一条 "Deploy reader to GitHub Pages" 等它变成 ✅ 绿勾（一般 1 分钟左右）
- [ ] 打开阅读网站，按 `Ctrl+Shift+R`（Mac：`Cmd+Shift+R`）强制刷新
  ✅ 首页出现新一期

---

## B. 脚本有修复后，重跑已经上传过的期刊

脚本修好一个 bug 后，只影响之后**重新运行**脚本生成的数据。之前已经上传的期刊要重跑一遍才能用上修复。

- [ ] 按 **A-1** 拿到最新版脚本
- [ ] 按 **A-2** 更新依赖
- [ ] 对每一期都重跑一次提取脚本。**文件名要和当初处理时完全一样**，这样才会覆盖原来那一期，而不是在网站上多出一个重复的期刊：
  - [ ] UK 8 August 2026
  - [ ] UK 5 September 2026
  - [ ] Europe 29 August 2026
  - [ ] Europe 12 September 2026
  - [ ] （以后新增的期刊继续往这里加）
- [ ] 按 **A-6**，把 `reader/data/` 里重新生成的这几个 `.js` 上传到 GitHub（同名文件会直接覆盖）；`manifest.js` 可以一起传
- [ ] 按 **A-7** 确认部署成功
- [ ] 告诉 Claude "已经重跑上传了"，Claude 会帮你检查数据里还有没有残留的问题

> UK 22 August 2026 这一期的 PDF 在仓库里，Claude 每次修复后会自己重跑，这一期不用你管。

---

## 常见问题

**网站上看不到新一期**
- 检查上传时分支是不是 `claude/economist-pdf-web-reader-zn8j39`。传到 `main` 不会触发网站更新。
- 检查 Actions 页那一条是不是绿勾；如果是红叉，把截图发给 Claude。
- 强制刷新浏览器（`Ctrl+Shift+R`）。

**上传时提示文件太大**
- 你可能传了 PDF，PDF 不需要上传，只传 `reader/data/` 里的 `.js` 文件。

**网站上同一期出现了两次**
- 两次处理时 PDF 文件名不一样，生成了两个不同的期刊。告诉 Claude，帮你删掉多余的那个。

**运行脚本报错 `No module named ...`**
- 重新执行 A-2 的 `pip install -r requirements.txt`。
